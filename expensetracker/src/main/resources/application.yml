server:
  port: 2879
spring:
  application:
    name: ExpenseTracker
  datasource:
    url: jdbc:mysql://localhost:3306/expensetrackerinfo
    username: root
    password: pass@word1
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true